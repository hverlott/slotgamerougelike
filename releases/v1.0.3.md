````markdown
# Release v1.0.3 (2025-12-20)

This release bumps the package to `v1.0.3` and updates documentation and text assets to use "spin" terminology where appropriate.

Highlights
- Version bump to `1.0.3`.
- Documentation: many docs updated to replace "slot" → "spin" in UI text and narrative.
- Misc: minor packaging metadata and mobile UX copy updates.

Notable Changes
- Updated `README.md`, multiple `docs/` pages, and `index.html` to use "spin" wording.
- Updated `package.json` description and bumped version to `1.0.3`.

How this release was made
- `standard-version --release-as 1.0.3` was used to bump version and update `CHANGELOG.md`.
- Tag `v1.0.3` pushed to remote.

Upgrade notes
- No breaking API changes expected. This is primarily copy/documentation updates.

Contributors
- Thanks to contributors for docs and UX updates.

````
